# Frontend AI_README

React + TS. Atomic component structure. Named exports only.

## Cross-directory dependencies
Shared utils from `packages/shared/src/utils`. Backend API at `apps/backend`.

## Styling
- Tailwind CSS only — no CSS Modules, no inline styles

# Rule
No EMOJI